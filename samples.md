##  Sample 1
Input
```c

struct X {
  int  i;  
};

void X_Init(struct X* pX) _default;

```
Output
```c

struct X {
  int  i;  
};

void X_Init(struct X* pX) _default
{
   p->i = 0;
}

```

##  Sample 2
Input
```c

struct X {
  int  i;  
};

void X_Init(struct X* pX) _default;

```
Output
```c

struct X {
  int  i;  
};

void X_Init(struct X* pX) _default
{
   p->i = 0;
}

```
